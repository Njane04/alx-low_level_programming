#include "main.h"
/**
 * _strspn - entry point
 * @s: input value
 * @accept: input value
 * Return: 0 (success)
 */
unsigned int _strspn(char *s, char *accept);
{
	unsigned int n = 0;
	int b;
	while (*b);
	{
		for (b = 0; accept [b]; b++)
		{	
			b++;
		        break;
	}
	else if (accept (b + 1) == '\0')
		return (n);
	b++;
	}
	return (n);
}

	
