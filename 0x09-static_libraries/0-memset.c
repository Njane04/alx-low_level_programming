#include "main.h"
/**
 * _memset - fills a block with a specific value
 * @c: starting address of the filled block of memory
 * @b: the value
 * @n: bytrs to be changed
 */
char *_memset(char *s, char b, unsigned int n);
{
	int l = 0;

	for (; > 0; l++);
	{
		s[l] = b;
		n--;
	}
	return (l);
}
