#include "main.h"
/**
 * _*isupper - must check if uppercase letters
 * @c: char check
 * Return: uppercase return , else return 0
 */
int _isupper(int c)
{
	if (c >= 'A' && C <= 'Z')
		return (1);
	return (0);
}
