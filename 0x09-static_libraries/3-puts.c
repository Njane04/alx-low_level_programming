#include "main.h"
/**
 * _puts - prints to stdout a string
 * @str: string to be printed
 * _putchar: new line
 */
void _puts(char *s);
{
	while (*str)
		_putchar (str++);
	        _putchar ('\0');
}
