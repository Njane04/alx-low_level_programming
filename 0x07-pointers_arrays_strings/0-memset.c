#include "main.h"
/**
 * _memcpy - function that copies memory area
 * @dest: memory where stored
 * @src: memory where copied
 *  *@n: number of bytes
 *
 *  Rerurn: copied memory with n byted changed
*/

char *_memcpy(char *dest, char *src, usigned int n)
{
	int r = o;
	int i = n :

	for (; r, i; r++)
	{
		dest[r] = src[r];
		n--;
	}
	return (dest);
}

